# 開発・リリース方針

## タスク
- Linear の登録先はチーム **Marufeuille**（チケット接頭辞: `MAR`）、プロジェクト **Xteink Read Later**。チケットの検索・作成時はこのプロジェクトを指定する。
- Linear の識別用 ID: team `e771cdee-d653-4e9b-bed7-1467f0a3f66b`、project `198daa99-f6d5-437b-87f1-46a6c654387b`。
- 原則として Linear のチケットを起点にし、目的・受け入れ条件を確認して実装する。ユーザーからの直接依頼も着手してよい。
- PR に対応するチケットの ID / リンク、変更内容、検証結果を記載する。追加の改善はチケット候補として切り出し、依頼の範囲を広げない。
- 既存の未コミット変更を勝手に上書き・取り込みしない。

## Worktree・PR の分割
- 変更作業は必ずタスク専用の Git worktree とブランチで行う。編集前に作業ディレクトリ・ブランチ・差分を確認し、共用チェックアウトで直接作業しない。同じタスクの専用 worktree があれば再利用してよい。
- 大きな変更や複数の論点を含む変更は、レビュー可能な責務・段階ごとに小さな PR に分割する。依存する変更は Stacked PR とし、後続 PR の base を直前の PR のブランチにする。独立した変更は別 PR にする。
- 各 PR に目的・対象範囲・依存する PR・マージ順を記載する。各段階でテストが通り、単独でデプロイされても既存機能を壊さない状態にする。
- Stacked PR は依存元から順にマージする。依存元のマージ後は後続 PR の base と差分を確認・更新し、最新コミットで CI と必要なレビューを再確認してから次をマージする。

## 実装・テスト
- 振る舞いを変更するたび、単体テストと E2E の両方で不足する検証を見直し、必要なケースを追加・更新する。
- バグ修正では再発を検出するテストを追加する。正常系だけでなく、変更に関係する異常系・境界値も検証する。
- E2E は利用者の主要な操作と期待結果を検証する。通常の CI は fixture / モックで再現可能に保ち、ライブ E2E は必要に応じて実施する。
- 実装変更では `npm run typecheck`、`npm run test:unit`、`npm run test:e2e` を実行する。文書のみの変更では内容・差分の確認でよいが、マージ時の CI 条件は省略しない。
- CI を通すためにテストを無効化したり、期待値や必須チェックを不当に緩めたりしない。実行できない検証は理由を明記する。

## マージ・リリース
- 原則として PR 経由で `main` にマージする。GitHub の merge gate（[docs/github-merge-gates.md](docs/github-merge-gates.md)）がこれを強制する。bypass や保護の無効化はしない。
- 次を満たしたら、追加のユーザー確認を挟まず AI がマージする。
  - チケット / 依頼の受け入れ条件を満たしている。
  - PR の最新コミットで必須チェック `merge-gate` が成功している。これは `typecheck, unit, e2e` が success のときだけ成功し、失敗・未実行・実行中・スキップは成功として扱わない。PR では走らないジョブ（`main` 専用の deploy など）のスキップはマージを妨げない。
  - GitHub が要求するレビューがあれば完了している。必須レビューが無い場合は、CI 成功をもってマージしてよい。
  - 未解決の不具合・レビュー指摘・マージ競合がない。
- 「PR まで」「マージしない」はマージしない明示的な制限である。制限が無い、または後からマージ許可が出た場合は、上記条件でマージする。
- 修正を追加したら、そのコミットに対する CI を再確認する。チェックやブランチ保護を迂回しない。
- 条件を満たしたら `gh pr merge --auto` で auto-merge / merge queue に載せる。ゲートが赤または pending ならマージは完了しない。
- 認証・権限、秘密情報、データ削除・移行、CI / デプロイ設定の変更は高リスクとして PR に根拠と復旧方針を記載する。安全性を判断できない場合はマージを止め、具体的な懸念を報告する。
- リリースは既存の GitHub Actions に任せ、通常の作業でローカルから本番デプロイしない。
- マージ後は対象コミットのデプロイ成功まで確認する。失敗した場合は完了扱いせず、原因と対応状況を報告する。
- Linear のチケットはリリース確認後に完了とし、PR・検証結果・デプロイ結果を記録する。
